apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: hello-world

resources:
  - namespace.yml
  - sealed-auth-secret.yml
  - deployment.yml
  - service.yml
  - ingress.yml
  - configmap.yml
  - middleware-auth.yml

commonLabels:
  app: hello-world
